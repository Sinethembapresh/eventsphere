/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['res.cloudinary.com', 'thumbs.dreamstime.com'], // Add cloudinary and dreamstime domains
  },
}

module.exports = nextConfig
